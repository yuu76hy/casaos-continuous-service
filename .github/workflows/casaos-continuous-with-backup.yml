name: CasaOS+VNC+Tailscale 持续服务（备份+续跑恢复版）
on:
  workflow_dispatch:
  repository_dispatch:
    types: [renew_vnc]

jobs:
  casaos-vnc-server:
    runs-on: ubuntu-24.04
    timeout-minutes: 350
    env:
      DISPLAY: :0
      VNCDIR: /home/runner/.vnc
      VNC_PORT: 5900
      MAX_RUN_MINUTES: 340
      FIXED_VNC_PWD: "123456789"
      CASAOS_PORT: 80
      NETDISK_ROOT: /home/runner/netdisk
      BACKUP_DIR: /home/runner/backup
      BACKUP_EXCLUDE: "--exclude=/proc --exclude=/sys --exclude=/tmp --exclude=/dev --exclude=/run --exclude=/var/run --exclude=/home/runner/actions-runner --exclude=/var/lib/docker/overlay2"
      RUNNER_PASSWORD: "runner123"
      CLEAN_INTERVAL: 3600
      # B2存储密钥
      B2_KEY_ID: "87dbd01ad00c"
      B2_APPLICATION_KEY: "005199ce79f1bd581cbfc54b2fb12ba6b5fd7d0fb6"
      B2_BACKUP_BUCKET: "casaos-backup"
      BACKUP_MARKER: "latest_backup.txt"  # 标记最新备份文件名的文件
    steps:
      - name: 系统环境初始化与依赖更新
        id: init_env
        continue-on-error: false
        run: |
          echo "===== 初始化系统环境 ====="
          sudo apt update -y 2>&1 | grep -v "restart" || { echo "系统更新失败"; exit 1; }
          sudo apt upgrade -y 2>&1 | grep -v "restart" || { echo "系统升级失败"; exit 1; }
          sudo apt install -y tar gzip rsync rclone tigervnc-standalone-server netcat-openbsd psmisc curl git wget vim nano python3 python3-pip ca-certificates gnupg lsb-release bc || { echo "依赖安装失败"; exit 1; }
          
          echo "runner:$RUNNER_PASSWORD" | sudo chpasswd
          sudo mkdir -p $BACKUP_DIR $NETDISK_ROOT
          sudo chown -R runner:runner $BACKUP_DIR $NETDISK_ROOT
          sudo chmod -R 755 $BACKUP_DIR $NETDISK_ROOT
          echo "===== 环境初始化完成 ====="

      - name: 配置对象存储（Backblaze B2 + rclone）【B2专属适配版】
        id: config_rclone
        continue-on-error: false
        run: |
          echo "===== 配置 Backblaze B2 对象存储 ====="
          if [ -z "$B2_KEY_ID" ] || [ -z "$B2_APPLICATION_KEY" ]; then
            echo "错误：B2存储密钥未配置"; exit 1;
          fi
          
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf << EOF
[b2_backend]
type = b2
account = $B2_KEY_ID
key = $B2_APPLICATION_KEY
hard_delete = true
timeout = 30s
retries = 3
EOF
          chmod 600 ~/.config/rclone/rclone.conf
          
          echo "===== 测试 B2 存储连接 ====="
          rclone mkdir b2_backend:${B2_BACKUP_BUCKET}/ || echo "备份目录已存在/创建失败（忽略）"
          if rclone lsd b2_backend:${B2_BACKUP_BUCKET}/ --verbose --retries 3; then
            echo "✅ B2对象存储连接成功"
            echo "OSS_CONNECT_STATUS=✅ 成功" >> $GITHUB_ENV
          else
            echo "❌ B2存储连接失败"; exit 1;
          fi
          echo "===== B2对象存储配置完成 ====="

      - name: 检查是否有历史备份（续跑恢复用）
        id: check_backup
        continue-on-error: false
        run: |
          echo "===== 检查历史备份 ====="
          # 下载备份标记文件（记录最新备份文件名）
          if rclone copy b2_backend:${B2_BACKUP_BUCKET}/${BACKUP_MARKER} $BACKUP_DIR/ --timeout 30s; then
            LATEST_BACKUP=$(cat $BACKUP_DIR/${BACKUP_MARKER})
            echo "找到最新备份：$LATEST_BACKUP"
            echo "LATEST_BACKUP=$LATEST_BACKUP" >> $GITHUB_ENV
            echo "HAS_BACKUP=true" >> $GITHUB_ENV
          else
            echo "未找到历史备份，将执行首次全量备份"
            echo "HAS_BACKUP=false" >> $GITHUB_ENV
          fi

      - name: 安装 Docker + CasaOS（恢复前先安装基础服务）
        id: install_services
        continue-on-error: true
        run: |
          echo "===== 安装 Docker 引擎 ====="
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/docker.gpg
          echo "deb [arch=$(dpkg --print-architecture)] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list >/dev/null
          sudo apt update -y 2>&1 | grep -v "restart" && sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
          sudo systemctl start docker && sudo systemctl enable docker
          
          echo "===== 安装 CasaOS ====="
          curl -fsSL https://get.casaos.io | sudo bash 2>&1 | grep -v "restart" || echo "警告：CasaOS安装失败"
          sleep 10 && sudo systemctl stop casaos  # 先停止，恢复数据后再启动
          sudo systemctl stop docker  # 停止Docker避免数据冲突
          echo "===== 基础服务安装完成（待恢复数据） ====="

      - name: 续跑恢复：从B2恢复最新全量备份
        id: restore_backup
        if: env.HAS_BACKUP == 'true'
        continue-on-error: false
        run: |
          echo "===== 恢复最新全量备份：${LATEST_BACKUP} ====="
          # 从B2下载备份文件并解压恢复（内存直传，不落地）
          rclone cat b2_backend:${B2_BACKUP_BUCKET}/${LATEST_BACKUP} --timeout 120s --retries 3 | sudo tar -zxf - -C / --exclude-from=<(echo "$BACKUP_EXCLUDE" | tr ' ' '\n') 2>/dev/null
          
          if [ $? -eq 0 ]; then
            echo "✅ 备份恢复成功！数据与上一次环境完全一致"
            # 恢复后重启服务
            sudo systemctl start docker
            sleep 5 && sudo systemctl start casaos
          else
            echo "❌ 备份恢复失败，将重新执行首次备份"
            echo "HAS_BACKUP=false" >> $GITHUB_ENV  # 重置为首次备份
          fi

      - name: 配置 VNC + Tailscale
        id: deploy_core
        continue-on-error: true
        run: |
          echo "===== 配置 VNC 服务 ====="
          mkdir -p $VNCDIR && chmod 700 $VNCDIR
          echo "$FIXED_VNC_PWD" | vncpasswd -f > $VNCDIR/passwd && chmod 600 $VNCDIR/passwd
          
          echo "#!/bin/bash" > $VNCDIR/xstartup
          echo "export DISPLAY=:0" >> $VNCDIR/xstartup
          echo "export XDG_RUNTIME_DIR=/tmp/runtime-runner" >> $VNCDIR/xstartup
          echo "mkdir -p \$XDG_RUNTIME_DIR && chmod 700 \$XDG_RUNTIME_DIR" >> $VNCDIR/xstartup
          echo "exec /usr/bin/xterm -geometry 140x40+0+0 -ls -title \"CasaOS服务\"" >> $VNCDIR/xstartup
          chmod +x $VNCDIR/xstartup
          
          pgrep -x Xvnc && vncserver -kill $DISPLAY || true
          vncserver $DISPLAY -localhost no -geometry 1280x720 -depth 16 -SecurityTypes VncAuth || echo "VNC启动失败"
          
          echo "===== 配置 Tailscale 组网 ====="
          curl -fsSL https://tailscale.com/install.sh | sudo sh 2>&1 | grep -v "restart" || echo "Tailscale安装失败"
          sudo systemctl enable --now tailscaled && sleep 3
          MAX_RETRIES=3
          while [ $MAX_RETRIES -gt 0 ]; do
            if sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=casaos-${{ github.run_id }} --accept-routes=false; then
              TAILSCALE_IP=$(sudo tailscale ip -4)
              break
            fi
            MAX_RETRIES=$((MAX_RETRIES-1)) && sleep 5
          done
          echo "TAILSCALE_IP=$TAILSCALE_IP" >> $GITHUB_ENV
          echo "===== 核心服务部署完成 ====="

      - name: 首次备份：执行单次全量备份（仅无历史备份时执行）
        id: full_backup_only
        if: env.HAS_BACKUP == 'false'
        continue-on-error: false
        run: |
          echo "===== 执行首次全量备份 ====="
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          FULL_BACKUP_NAME="casaos_full_${TIMESTAMP}.tar.gz"
          
          # 内存直传全量备份至B2
          sudo tar -zcf - $BACKUP_EXCLUDE / --wildcards "*/casaos/*" "*/docker/*" 2>/dev/null | rclone rcat b2_backend:${B2_BACKUP_BUCKET}/${FULL_BACKUP_NAME} --timeout 60s --retries 3
          
          if [ $? -eq 0 ]; then
            echo "✅ 首次全量备份成功：b2_backend:${B2_BACKUP_BUCKET}/${FULL_BACKUP_NAME}"
            # 写入备份标记文件（供后续续跑恢复）
            echo "${FULL_BACKUP_NAME}" > $BACKUP_DIR/${BACKUP_MARKER}
            rclone copy $BACKUP_DIR/${BACKUP_MARKER} b2_backend:${B2_BACKUP_BUCKET}/ --timeout 30s
          else
            echo "❌ 首次备份失败，但不影响服务运行"
          fi
          echo "===== 首次备份执行完成 ====="

      - name: 输出详细服务状态信息
        id: output_info
        continue-on-error: false
        run: |
          echo -e "\n========================================"
          echo -e " 🚀 服务部署完成 - 备份+续跑恢复版"
          echo -e "========================================"
          echo -e " 🖥️  系统信息 | 可用空间: $(df -h / | awk 'NR==2{print $4}')"
          echo -e "   - runner密码: \033[1;32m${RUNNER_PASSWORD}\033[0m"
          echo -e "   - VNC密码: \033[1;32m${FIXED_VNC_PWD}\033[0m"
          echo -e " 📦 对象存储 | 状态: ${OSS_CONNECT_STATUS}（Backblaze B2）"
          echo -e " 🔗 访问地址 | VNC: ${TAILSCALE_IP:-未获取}:${VNC_PORT} | CasaOS: http://${TAILSCALE_IP:-未获取}:${CASAOS_PORT}"
          echo -e " 📋 备份策略 | 仅首次全量备份，续跑自动恢复"
          echo -e " 📌 数据状态 | $(if [ $HAS_BACKUP == 'true' ]; then echo '已恢复上一次环境数据'; else echo '首次部署，已创建基础备份'; fi)"
          echo -e " 🧹 清理策略 | 每1小时自动清除垃圾数据"
          echo -e "========================================\n"

      - name: 保活+续跑+定时清理核心逻辑
        id: core_logic
        continue-on-error: true
        run: |
          echo "===== 启动保活+定时清理监控 ====="
          start_time=$(date +%s)
          last_clean_time=$start_time

          # 每小时垃圾清理（深度清理）
          auto_clean() {
            echo "===== 开始每小时垃圾清理 ====="
            BEFORE_CLEAN=$(df -h / | awk 'NR==2{print $4}')
            sudo apt clean -y && sudo apt autoremove -y --purge
            sudo docker system prune -af --volumes 2>/dev/null || true
            sudo rm -rf /tmp/* /var/tmp/* /home/runner/.cache/* 2>/dev/null
            AFTER_CLEAN=$(df -h / | awk 'NR==2{print $4}')
            echo "清理前可用空间：${BEFORE_CLEAN} | 清理后可用空间：${AFTER_CLEAN}"
          }

          # 主循环：保活+定时清理+续跑触发
          while true; do
            current_time=$(date +%s)
            run_minutes=$(( (current_time - start_time) / 60 ))
            FREE_SPACE=$(df -h / | awk 'NR==2{print $4}' | sed 's/[^0-9.]//g')
            
            # 1. 每小时执行垃圾清理
            if [ $((current_time - last_clean_time)) -ge $CLEAN_INTERVAL ]; then
              auto_clean
              last_clean_time=$current_time
            fi

            # 2. 空间应急清理（不足1GB立即清理）
            if (( $(echo "$FREE_SPACE < 1" | bc -l) )); then
              echo "⚠️  空间不足1GB，紧急清理..."
              auto_clean
              last_clean_time=$current_time
            fi

            # 3. 剩余10分钟触发续跑（不重复备份，仅续跑恢复）
            if [ $((MAX_RUN_MINUTES - run_minutes)) -eq 10 ]; then
              echo "===== 剩余10分钟，触发续跑 ====="
              curl -X POST https://api.github.com/repos/${{ github.repository }}/dispatches \
                -H "Accept: application/vnd.github+json" \
                -H "Authorization: Bearer ${{ secrets.USER_GITHUB_PAT }}" \
                -d '{"event_type":"renew_vnc"}' \
                --connect-timeout 30s || echo "❌ 续跑触发失败"
            fi

            # 4. 服务健康检查
            pgrep -x Xvnc || { echo "⚠️ VNC异常，重启"; vncserver $DISPLAY -localhost no -geometry 1280x720 -depth 16 -SecurityTypes VncAuth; }
            sudo systemctl is-active --quiet docker || { echo "⚠️ Docker异常，重启"; sudo systemctl restart docker; }
            sudo systemctl is-active --quiet casaos || { echo "⚠️ CasaOS异常，重启"; sudo systemctl restart casaos; }
            sudo systemctl is-active --quiet tailscaled || { echo "⚠️ Tailscale异常，重启"; sudo systemctl restart tailscaled; }

            # 运行状态日志
            echo "[$(date)] 运行时长：${run_minutes}分钟 | 剩余空间: $(df -h / | awk 'NR==2{print $4}') | 下次清理：$(( (last_clean_time + CLEAN_INTERVAL - current_time) / 60 ))分钟后"
            sleep 300
          done

      - name: 优雅清理环境
        id: clean_env
        if: always()
        run: |
          echo "===== 清理环境 ====="
          # 停止服务
          vncserver -kill $DISPLAY || true && pkill -9 Xvnc || true
          sudo systemctl stop docker casaos tailscaled || true
          sudo tailscale down || true
          # 清理本地文件（备份已存B2）
          rm -rf $BACKUP_DIR $VNCDIR $NETDISK_ROOT || true
          echo "===== 环境清理完成 ====="
