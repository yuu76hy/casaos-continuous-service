name: CasaOS+VNC+Tailscale 持续服务（适配Path-style存储版）
on:
  workflow_dispatch:
  repository_dispatch:
    types: [renew_vnc]

jobs:
  casaos-vnc-server:
    runs-on: ubuntu-24.04
    timeout-minutes: 350
    env:
      DISPLAY: :0
      VNCDIR: /home/runner/.vnc
      VNC_PORT: 5900
      MAX_RUN_MINUTES: 340
      FIXED_VNC_PWD: "123456789"
      CASAOS_PORT: 80
      NETDISK_ROOT: /home/runner/netdisk
      BACKUP_DIR: /home/runner/backup
      INC_BACKUP_BASE: "casaos_base.tar.gz"
      KEEP_BACKUP_NUM: 2
      BACKUP_EXCLUDE: "--exclude=/proc --exclude=/sys --exclude=/tmp --exclude=/dev --exclude=/run --exclude=/var/run --exclude=/home/runner/actions-runner --exclude=/var/lib/docker/overlay2"
      RUNNER_PASSWORD: "runner123"
      CLEAN_INTERVAL: 3600
    steps:
      - name: 系统环境初始化与依赖更新
        id: init_env
        continue-on-error: false
        run: |
          echo "===== 初始化系统环境 ====="
          sudo apt update -y 2>&1 | grep -v "restart" || { echo "系统更新失败"; exit 1; }
          sudo apt upgrade -y 2>&1 | grep -v "restart" || { echo "系统升级失败"; exit 1; }
          sudo apt install -y tar gzip rsync rclone tigervnc-standalone-server netcat-openbsd psmisc curl git wget vim nano python3 python3-pip ca-certificates gnupg lsb-release || { echo "依赖安装失败"; exit 1; }
          
          echo "runner:$RUNNER_PASSWORD" | sudo chpasswd
          sudo mkdir -p $BACKUP_DIR $NETDISK_ROOT
          sudo chown -R runner:runner $BACKUP_DIR $NETDISK_ROOT
          sudo chmod -R 755 $BACKUP_DIR $NETDISK_ROOT
          echo "===== 环境初始化完成 ====="

      - name: 配置对象存储（rclone）【适配Path-style】
        id: config_rclone
        continue-on-error: false
        env:
          # 直接填入你的存储密钥（也可改回Secrets，见下方说明）
          OSS_AK: "o57570wv"
          OSS_SK: "5g2m47tkhldphlzz"
          OSS_ENDPOINT: "objectstorageapi.ap-northeast-1.clawcloudrun.com"
        run: |
          echo "===== 配置Path-style对象存储 ====="
          # 验证参数是否为空
          if [ -z "$OSS_AK" ] || [ -z "$OSS_SK" ] || [ -z "$OSS_ENDPOINT" ]; then
            echo "错误：存储密钥/地址未配置"; exit 1;
          fi
          
          mkdir -p ~/.config/rclone
          # 核心：添加path_style = true 适配Path-style请求
          cat > ~/.config/rclone/rclone.conf << EOF
[oss]
type = s3
provider = Other
endpoint = $OSS_ENDPOINT
access_key_id = $OSS_AK
secret_access_key = $OSS_SK
path_style = true  # 必须开启：适配Path-style请求
acl = private
EOF
          chmod 600 ~/.config/rclone/rclone.conf
          
          echo "===== 测试存储连接 ====="
          # 先创建备份目录（避免目录不存在报错）
          rclone mkdir oss:casaos-backup/
          if rclone lsd oss:casaos-backup/ --verbose; then
            echo "对象存储连接成功（Path-style模式）"
            echo "OSS_CONNECT_STATUS=✅ 成功" >> $GITHUB_ENV
          else
            echo "错误：存储连接失败，请检查："
            echo "1. 公网地址：$OSS_ENDPOINT 是否可访问"
            echo "2. AK/SK 是否正确"
            exit 1
          fi
          echo "===== 对象存储配置完成 ====="

      - name: 恢复增量备份索引（仅索引，无数据）
        id: restore_backup_index
        continue-on-error: true
        run: |
          echo "===== 恢复增量备份索引 ====="
          rclone copy oss:casaos-backup/backup.snar $BACKUP_DIR/ || { echo "无历史增量索引，跳过恢复"; exit 0; }
          rclone copy oss:casaos-backup/current_full_backup.txt $BACKUP_DIR/ || true
          echo "增量备份索引恢复完成"

      - name: 安装 Docker + CasaOS
        id: install_services
        continue-on-error: true
        run: |
          echo "===== 安装 Docker 引擎 ====="
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/docker.gpg
          echo "deb [arch=$(dpkg --print-architecture)] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list >/dev/null
          sudo apt update -y 2>&1 | grep -v "restart" && sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
          sudo systemctl start docker && sudo systemctl enable docker
          
          echo "===== 安装 CasaOS ====="
          curl -fsSL https://get.casaos.io | sudo bash 2>&1 | grep -v "restart" || echo "警告：CasaOS安装失败"
          sleep 10 && sudo systemctl start casaos || echo "CasaOS启动失败"
          echo "===== 服务安装完成 ====="

      - name: 配置 VNC + Tailscale
        id: deploy_core
        continue-on-error: true
        run: |
          echo "===== 配置 VNC 服务 ====="
          mkdir -p $VNCDIR && chmod 700 $VNCDIR
          echo "$FIXED_VNC_PWD" | vncpasswd -f > $VNCDIR/passwd && chmod 600 $VNCDIR/passwd
          
          echo "#!/bin/bash" > $VNCDIR/xstartup
          echo "export DISPLAY=:0" >> $VNCDIR/xstartup
          echo "export XDG_RUNTIME_DIR=/tmp/runtime-runner" >> $VNCDIR/xstartup
          echo "mkdir -p \$XDG_RUNTIME_DIR && chmod 700 \$XDG_RUNTIME_DIR" >> $VNCDIR/xstartup
          echo "exec /usr/bin/xterm -geometry 140x40+0+0 -ls -title \"CasaOS服务\"" >> $VNCDIR/xstartup
          chmod +x $VNCDIR/xstartup
          
          pgrep -x Xvnc && vncserver -kill $DISPLAY || true
          vncserver $DISPLAY -localhost no -geometry 1280x720 -depth 16 -SecurityTypes VncAuth || echo "VNC启动失败"
          
          echo "===== 配置 Tailscale 组网 ====="
          curl -fsSL https://tailscale.com/install.sh | sudo sh 2>&1 | grep -v "restart" || echo "Tailscale安装失败"
          sudo systemctl enable --now tailscaled && sleep 3
          MAX_RETRIES=3
          while [ $MAX_RETRIES -gt 0 ]; do
            if sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=casaos-${{ github.run_id }} --accept-routes=false; then
              TAILSCALE_IP=$(sudo tailscale ip -4)
              break
            fi
            MAX_RETRIES=$((MAX_RETRIES-1)) && sleep 5
          done
          echo "TAILSCALE_IP=$TAILSCALE_IP" >> $GITHUB_ENV
          echo "===== 核心服务部署完成 ====="

      - name: 输出详细服务状态信息
        id: output_info
        continue-on-error: false
        run: |
          echo -e "\n========================================"
          echo -e " 🚀 服务部署完成 - 详细状态汇总"
          echo -e "========================================"
          echo -e " 🖥️  系统信息 | 可用空间: $(df -h / | awk 'NR==2{print $4}')"
          echo -e "   - runner密码: \033[1;32m${RUNNER_PASSWORD}\033[0m"
          echo -e " 📦 对象存储 | 状态: ${OSS_CONNECT_STATUS}（Path-style模式）"
          echo -e " 🔗 访问地址 | VNC: ${TAILSCALE_IP:-未获取}:${VNC_PORT} | CasaOS: http://${TAILSCALE_IP:-未获取}:${CASAOS_PORT}"
          echo -e " 📋 备份策略 | 全量+增量（内存直传，不落地）"
          echo -e " 🧹 清理策略 | 每1小时自动扫描并清除垃圾数据"
          echo -e "========================================\n"

      - name: 保活+续跑+内存直传备份+定时清理核心逻辑
        id: core_logic
        continue-on-error: true
        run: |
          echo "===== 启动保活+备份+定时清理监控 ====="
          start_time=$(date +%s)
          last_backup_time=$start_time
          last_clean_time=$start_time

          full_backup() {
            echo "===== 内存直传 - 创建全量备份 ====="
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            FULL_BACKUP_NAME="casaos_full_${TIMESTAMP}.tar.gz"
            sudo tar -zcf - $BACKUP_EXCLUDE / --wildcards "*/casaos/*" "*/docker/*" 2>/dev/null | rclone rcat oss:casaos-backup/${FULL_BACKUP_NAME}
            if [ $? -eq 0 ]; then
              echo "全量备份内存直传成功：oss:casaos-backup/${FULL_BACKUP_NAME}"
              echo "${FULL_BACKUP_NAME}" > $BACKUP_DIR/current_full_backup.txt
              rclone copy $BACKUP_DIR/current_full_backup.txt oss:casaos-backup/
            else
              echo "全量备份直传失败"
            fi
          }

          inc_backup() {
            echo "===== 内存直传 - 创建增量备份 ====="
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            INC_BACKUP_NAME="casaos_inc_${TIMESTAMP}.tar.gz"
            sudo tar -zcf - $BACKUP_EXCLUDE / --listed-incremental=$BACKUP_DIR/backup.snar --wildcards "*/casaos/*" "*/docker/*" 2>/dev/null | rclone rcat oss:casaos-backup/${INC_BACKUP_NAME}
            if [ $? -eq 0 ]; then
              echo "增量备份内存直传成功：oss:casaos-backup/${INC_BACKUP_NAME}"
              rclone copy $BACKUP_DIR/backup.snar oss:casaos-backup/
              rclone ls oss:casaos-backup/ --include "casaos_inc_*.tar.gz" | sort -k2 | head -n -${KEEP_BACKUP_NUM} | awk '{print $2}' | xargs -I {} rclone delete oss:casaos-backup/{}
            else
              echo "增量备份直传失败"
            fi
          }

          auto_clean() {
            echo "===== 开始每小时垃圾清理 ====="
            BEFORE_CLEAN=$(df -h / | awk 'NR==2{print $4}')
            sudo apt clean -y
            sudo apt autoremove -y --purge
            sudo docker system prune -af --volumes 2>/dev/null || true
            sudo rm -rf /tmp/* /var/tmp/* 2>/dev/null
            sudo rm -rf /home/runner/.cache/* 2>/dev/null
            sudo rm -rf /home/runner/actions-runner/cached/_diag/* 2>/dev/null
            sudo rm -rf /home/runner/actions-runner/_work/_temp/* 2>/dev/null
            sudo rm -rf /tmp/runtime-runner/* 2>/dev/null
            AFTER_CLEAN=$(df -h / | awk 'NR==2{print $4}')
            echo "===== 垃圾清理完成 ====="
            echo "清理前可用空间：${BEFORE_CLEAN} | 清理后可用空间：${AFTER_CLEAN}"
          }

          [ ! -f "$BACKUP_DIR/current_full_backup.txt" ] && full_backup || inc_backup

          while true; do
            current_time=$(date +%s)
            run_minutes=$(( (current_time - start_time) / 60 ))
            FREE_SPACE=$(df -h / | awk 'NR==2{print $4}' | sed 's/[^0-9.]//g')
            
            if [ $((current_time - last_clean_time)) -ge $CLEAN_INTERVAL ]; then
              auto_clean
              last_clean_time=$current_time
            fi

            if (( $(echo "$FREE_SPACE < 1" | bc -l) )); then
              echo "⚠️  空间不足1GB，紧急清理..."
              auto_clean
              last_clean_time=$current_time
            fi

            if [ $((MAX_RUN_MINUTES - run_minutes)) -eq 10 ]; then
              echo "===== 剩余10分钟，触发续跑 ====="
              full_backup
              curl -X POST https://api.github.com/repos/${{ github.repository }}/dispatches \
                -H "Accept: application/vnd.github+json" \
                -H "Authorization: Bearer ${{ secrets.USER_GITHUB_PAT }}" \
                -d '{"event_type":"renew_vnc"}' || echo "续跑触发失败"
            fi

            if [ $((current_time - last_backup_time)) -ge 3600 ]; then
              inc_backup
              last_backup_time=$current_time
            fi

            pgrep -x Xvnc || { echo "VNC异常，重启"; vncserver $DISPLAY -localhost no -geometry 1280x720 -depth 16 -SecurityTypes VncAuth; }
            sudo systemctl is-active --quiet docker || { echo "Docker异常，重启"; sudo systemctl restart docker; }
            sudo systemctl is-active --quiet casaos || { echo "CasaOS异常，重启"; sudo systemctl restart casaos; }
            sudo systemctl is-active --quiet tailscaled || { echo "Tailscale异常，重启"; sudo systemctl restart tailscaled; }

            echo "[$(date)] 运行${run_minutes}分钟 | 剩余空间: $(df -h / | awk 'NR==2{print $4}') | 下次清理：$(( (last_clean_time + CLEAN_INTERVAL - current_time) / 60 ))分钟后"
            sleep 300
          done

      - name: 优雅清理环境
        id: clean_env
        if: always()
        run: |
          echo "===== 清理环境 ====="
          vncserver -kill $DISPLAY || true && pkill -9 Xvnc || true
          sudo systemctl stop docker casaos tailscaled || true
          sudo tailscale down || true
          rm -rf $BACKUP_DIR $VNCDIR $NETDISK_ROOT || true
          echo "===== 环境清理完成 ====="
