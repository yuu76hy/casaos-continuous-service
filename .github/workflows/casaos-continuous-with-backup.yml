name: CasaOS-Tailscale-简单部署

on:
  workflow_dispatch:
  repository_dispatch:
    types: [renew_casaos_tailscale]

jobs:
  deploy-casaos:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    
    env:
      MAX_RUN_MINUTES: 360
      WEBDAV_MOUNT_POINT: /mnt/webdav
      WEBDAV_REMOTE_PATH: /z/Ubu
      BACKUP_COMPRESSION: zstd
      SNAPSHOT_RETENTION: 3
      DEBUG_USER: roots
      MAX_MOUNT_RETRIES: 3
      RETRY_DELAY: 15

    steps:
      - name: 步骤1 - 系统环境初始化
        run: |
          echo "===== 步骤1: 系统环境初始化 ====="
          echo "开始时间: $(date)"
          sudo apt update -y
          sudo apt install -y curl wget git jq tar gzip zstd rsync fuse3 psmisc net-tools iputils-ping ca-certificates lsb-release
          echo "安装 rclone..."
          curl -s https://rclone.org/install.sh | sudo bash
          echo "user_allow_other" | sudo tee -a /etc/fuse.conf
          sudo mkdir -p ${{ env.WEBDAV_MOUNT_POINT }}
          sudo chmod 777 ${{ env.WEBDAV_MOUNT_POINT }}
          echo "✅ 系统初始化完成"

      - name: 步骤2 - 创建调试用户
        run: |
          echo "===== 步骤2: 创建调试用户 ====="
          DEBUG_PASSWORD="${{ secrets.DEBUG_PASSWORD }}"
          if [ -z "$DEBUG_PASSWORD" ]; then
            echo "⚠️ 未配置DEBUG_PASSWORD，使用默认密码（仅测试用）"
            DEBUG_PASSWORD="roots1234"
          fi
          if id "${{ env.DEBUG_USER }}" &>/dev/null; then
            echo "用户已存在，修改密码"
            echo "${{ env.DEBUG_USER }}:$DEBUG_PASSWORD" | sudo chpasswd
          else
            sudo useradd -m -s /bin/bash "${{ env.DEBUG_USER }}"
            echo "${{ env.DEBUG_USER }}:$DEBUG_PASSWORD" | sudo chpasswd
            sudo usermod -aG sudo "${{ env.DEBUG_USER }}"
          fi
          echo "✅ 调试用户创建完成"

      - name: 步骤3 - 挂载 WebDAV（带重试）
        id: mount_webdav
        run: |
          echo "===== 步骤3: 挂载 WebDAV ====="
          WEBDAV_URL="${{ secrets.WEBDAV_URL }}"
          WEBDAV_USER="${{ secrets.WEBDAV_USER }}"
          WEBDAV_PASSWORD="${{ secrets.WEBDAV_PASSWORD }}"
          if [ -z "$WEBDAV_URL" ] || [ -z "$WEBDAV_USER" ] || [ -z "$WEBDAV_PASSWORD" ]; then
            echo "❌ WebDAV 凭据未配置，跳过挂载"
            echo "WEBDAV_AVAILABLE=false" >> $GITHUB_ENV
            exit 0
          fi
          WEBDAV_FULL_URL="${WEBDAV_URL}${{ env.WEBDAV_REMOTE_PATH }}"
          echo "WebDAV完整URL: $WEBDAV_FULL_URL"
          # ========== 第70行核心修正 ==========
          # 关键：EOF前后无多余空格，且配置内容缩进不影响rclone解析
          cat > /tmp/rclone.conf <<EOF
[webdav]
type = webdav
url = $WEBDAV_FULL_URL
vendor = other
user = $WEBDAV_USER
pass = $WEBDAV_PASSWORD
EOF
          # ====================================
          CONNECT_SUCCESS=false
          for i in $(seq 1 ${{ env.MAX_MOUNT_RETRIES }}); do
            echo "测试连接（第$i次）..."
            if rclone lsd webdav: --config=/tmp/rclone.conf; then
              CONNECT_SUCCESS=true
              echo "✅ WebDAV 连接成功"
              break
            fi
            echo "❌ 连接失败，${{ env.RETRY_DELAY }}秒后重试"
            sleep ${{ env.RETRY_DELAY }}
          done
          if [ "$CONNECT_SUCCESS" = "false" ]; then
            echo "❌ WebDAV 连接失败（重试${{ env.MAX_MOUNT_RETRIES }}次）"
            echo "WEBDAV_AVAILABLE=false" >> $GITHUB_ENV
            exit 0
          fi
          echo "挂载 WebDAV..."
          rclone mount webdav: "${{ env.WEBDAV_MOUNT_POINT }}" \
            --config=/tmp/rclone.conf \
            --vfs-cache-mode writes \
            --allow-other \
            --daemon \
            --log-level INFO \
            --log-file /tmp/rclone.log
          sleep 5
          if mountpoint -q "${{ env.WEBDAV_MOUNT_POINT }}"; then
            echo "✅ WebDAV 挂载成功"
            echo "WEBDAV_AVAILABLE=true" >> $GITHUB_ENV
            sudo mkdir -p "${{ env.WEBDAV_MOUNT_POINT }}/backups/snapshots"
            sudo chmod 777 "${{ env.WEBDAV_MOUNT_POINT }}/backups/snapshots"
          else
            echo "❌ WebDAV 挂载失败"
            echo "WEBDAV_AVAILABLE=false" >> $GITHUB_ENV
          fi

      - name: 步骤4 - 智能恢复系统状态
        if: env.WEBDAV_AVAILABLE == 'true'
        run: |
          echo "===== 步骤4: 智能恢复系统状态 ====="
          find_snapshot() {
            local snapshot_dir="${{ env.WEBDAV_MOUNT_POINT }}/backups/snapshots"
            if [ -d "$snapshot_dir" ]; then
              find "$snapshot_dir" -maxdepth 1 -type d -name "2*" 2>/dev/null | sort -r | head -1
            fi
          }
          LATEST_SNAPSHOT=$(find_snapshot)
          if [ -n "$LATEST_SNAPSHOT" ] && [ -f "$LATEST_SNAPSHOT/snapshot_info.json" ]; then
            echo "找到最新快照: $(basename $LATEST_SNAPSHOT)"
            SNAPSHOT_INFO=$(cat "$LATEST_SNAPSHOT/snapshot_info.json")
            SNAPSHOT_TIME=$(echo "$SNAPSHOT_INFO" | jq -r '.timestamp' 2>/dev/null || echo "未知")
            echo "快照时间: $SNAPSHOT_TIME"
            sudo systemctl stop casaos docker 2>/dev/null || true
            sleep 3
            restore_from_tar() {
              local tar_file="$1"
              local target_dir="$2"
              if [ -f "$tar_file" ]; then
                echo "恢复: $(basename $tar_file)"
                if [[ "$tar_file" == *.zst ]]; then
                  sudo tar -I 'zstd -d' -xf "$tar_file" -C "$target_dir" 2>/dev/null && return 0 || return 1
                elif [[ "$tar_file" == *.gz ]]; then
                  sudo tar -xzf "$tar_file" -C "$target_dir" 2>/dev/null && return 0 || return 1
                fi
              fi
              return 1
            }
            DOCKER_RESTORED=false
            if restore_from_tar "$LATEST_SNAPSHOT/docker_state.tar.${{ env.BACKUP_COMPRESSION }}" "/"; then
              echo "✅ Docker 数据恢复成功"
              DOCKER_RESTORED=true
            else
              echo "❌ Docker 数据恢复失败"
            fi
            CASAOS_RESTORED=false
            if restore_from_tar "$LATEST_SNAPSHOT/casaos_state.tar.${{ env.BACKUP_COMPRESSION }}" "/"; then
              echo "✅ CasaOS 数据恢复成功"
              CASAOS_RESTORED=true
            else
              echo "❌ CasaOS 数据恢复失败"
            fi
            if [ "$DOCKER_RESTORED" = "true" ] || [ "$CASAOS_RESTORED" = "true" ]; then
              echo "✅ 系统状态恢复完成"
              echo "RESTORED_FROM_BACKUP=true" >> $GITHUB_ENV
            else
              echo "❌ 系统状态恢复失败"
              echo "RESTORED_FROM_BACKUP=false" >> $GITHUB_ENV
            fi
          else
            echo "ℹ️ 未找到可用快照，将执行全新安装"
            echo "RESTORED_FROM_BACKUP=false" >> $GITHUB_ENV
          fi

      - name: 步骤5 - 安装/恢复 CasaOS
        run: |
          echo "===== 步骤5: 安装 CasaOS ====="
          if [ "$RESTORED_FROM_BACKUP" = "true" ]; then
            echo "✅ 从备份恢复，验证组件..."
            if ! command -v docker &>/dev/null; then
              echo "🐳 安装 Docker..."
              curl -fsSL https://get.docker.com | sudo bash
            else
              echo "✅ Docker 已安装"
            fi
            if [ ! -f "/usr/local/bin/casaos" ]; then
              echo "🏠 安装 CasaOS..."
              curl -fsSL https://get.casaos.io | sudo bash
            else
              echo "✅ CasaOS 已安装"
            fi
          else
            echo "🆕 执行全新安装..."
            curl -fsSL https://get.docker.com | sudo bash
            curl -fsSL https://get.casaos.io | sudo bash
          fi
          sudo systemctl daemon-reload
          sudo systemctl enable --now docker casaos
          sleep 10
          for i in $(seq 1 30); do
            if curl -s http://localhost:80 &>/dev/null; then
              echo "✅ CasaOS Web 界面可访问"
              break
            fi
            sleep 2
          done
          echo "📊 安装验证:"
          echo "  🐳 Docker: $(docker --version 2>/dev/null || echo '未安装')"
          echo "  🏠 CasaOS: $(sudo casaos version 2>/dev/null || echo '未安装')"

      - name: 步骤6 - 部署 Tailscale（带重试）
        run: |
          echo "===== 步骤6: 部署 Tailscale ====="
          TAILSCALE_AUTH_KEY="${{ secrets.TAILSCALE_AUTH_KEY }}"
          if [ -z "$TAILSCALE_AUTH_KEY" ]; then
            echo "⚠️ 未配置 TAILSCALE_AUTH_KEY，跳过组网"
            echo "TAILSCALE_CONNECTED=false" >> $GITHUB_ENV
            exit 0
          fi
          curl -fsSL https://tailscale.com/install.sh | sudo bash
          TS_CONNECTED=false
          for i in $(seq 1 2); do
            sudo tailscale up \
              --authkey="$TAILSCALE_AUTH_KEY" \
              --accept-routes \
              --hostname="casaos-runner-${{ github.run_id }}" \
              --accept-dns=false \
              --reset
            sleep 5
            TAILSCALE_IP=$(sudo tailscale ip -4 2>/dev/null || echo "")
            if [ -n "$TAILSCALE_IP" ]; then
              TS_CONNECTED=true
              break
            fi
            echo "⚠️ Tailscale连接失败，10秒后重试"
            sudo tailscale down 2>/dev/null
            sleep 10
          done
          if [ "$TS_CONNECTED" = "true" ]; then
            echo "✅ Tailscale 连接成功"
            echo "🌐 Tailscale IP: $TAILSCALE_IP"
            echo "TAILSCALE_CONNECTED=true" >> $GITHUB_ENV
            echo "TAILSCALE_IP=$TAILSCALE_IP" >> $GITHUB_ENV
          else
            echo "❌ Tailscale 连接失败"
            echo "TAILSCALE_CONNECTED=false" >> $GITHUB_ENV
          fi

      - name: 步骤7 - 输出访问信息
        run: |
          echo "===== 步骤7: 访问信息 ====="
          PUBLIC_IP=$(curl -s --max-time 3 https://api.ipify.org || echo "未知")
          LOCAL_IP=$(hostname -I | awk '{print $1}' 2>/dev/null || echo "未知")
          DEBUG_PASSWORD="${{ secrets.DEBUG_PASSWORD }}"
          [ -z "$DEBUG_PASSWORD" ] && DEBUG_PASSWORD="roots1234"
          echo "========================================"
          echo "🎉 CasaOS 部署完成"
          echo "========================================"
          echo ""
          echo "🌍 公网访问: http://$PUBLIC_IP"
          echo "🏠 本地访问: http://$LOCAL_IP"
          echo ""
          if [ "$TAILSCALE_CONNECTED" = "true" ]; then
            echo "🔒 Tailscale 访问:"
            echo "  http://$TAILSCALE_IP"
            echo "  http://casaos-runner-${{ github.run_id }}.tailscale"
            echo ""
          fi
          echo "🔧 管理访问:"
          echo "  SSH: ssh ${{ env.DEBUG_USER }}@$LOCAL_IP"
          echo "  密码: $DEBUG_PASSWORD"
          echo ""
          echo "📊 系统状态:"
          echo "  🐳 Docker: $(systemctl is-active docker 2>/dev/null || echo 'unknown')"
          echo "  🏠 CasaOS: $(systemctl is-active casaos 2>/dev/null || echo 'unknown')"
          echo "  💾 数据来源: $( [ "$RESTORED_FROM_BACKUP" = "true" ] && echo 从备份恢复 || echo 全新安装 )"
          echo "  ⏰ 备份计划: 300分钟时执行完整备份"
          echo ""
          echo "⚠️ 重要提示:"
          echo "  1. 立即修改默认密码！"
          echo "  2. 通过 Tailscale 访问更安全"
          echo "========================================"

      - name: 步骤8 - 自动备份与工作流续期
        run: |
          echo "===== 步骤8: 完整备份与续期 ====="
          START_TIME=$(date +%s)
          BACKUP_DONE=false
          full_backup() {
            local timestamp=$(date +%Y%m%d_%H%M%S)
            local backup_dir="${{ env.WEBDAV_MOUNT_POINT }}/backups/snapshots/$timestamp"
            echo "💾 执行全量备份（时间戳: $timestamp）..."
            sudo mkdir -p "$backup_dir" && sudo chmod 777 "$backup_dir"
            echo "⏸️ 停止 Docker 和 CasaOS 服务..."
            sudo systemctl stop casaos docker 2>/dev/null || true
            sleep 5
            echo "🐳 备份 Docker 数据..."
            if [ "${{ env.BACKUP_COMPRESSION }}" = "zstd" ]; then
              sudo tar -I 'zstd -T0 -3' -cf "$backup_dir/docker_state.tar.zst" \
                /var/lib/docker /etc/docker \
                --exclude="*.log" --exclude="*.tmp" --exclude="*.cache" --exclude="/var/lib/docker/tmp" 2>/dev/null
            else
              sudo tar -czf "$backup_dir/docker_state.tar.gz" \
                /var/lib/docker /etc/docker \
                --exclude="*.log" --exclude="*.tmp" --exclude="*.cache" --exclude="/var/lib/docker/tmp" 2>/dev/null
            fi
            echo "🏠 备份 CasaOS 数据..."
            if [ "${{ env.BACKUP_COMPRESSION }}" = "zstd" ]; then
              sudo tar -I 'zstd -T0 -3' -cf "$backup_dir/casaos_state.tar.zst" \
                /var/lib/casaos /etc/casaos /usr/local/bin/casaos* \
                --exclude="*.log" --exclude="*.tmp" --exclude="*.cache" 2>/dev/null
            else
              sudo tar -czf "$backup_dir/casaos_state.tar.gz" \
                /var/lib/casaos /etc/casaos /usr/local/bin/casaos* \
                --exclude="*.log" --exclude="*.tmp" --exclude="*.cache" 2>/dev/null
            fi
            echo "▶️ 重新启动服务..."
            sudo systemctl start docker && sleep 5 && sudo systemctl start casaos
            cat > "$backup_dir/restore.sh" <<'RESTORE_EOF'
#!/bin/bash
echo "开始恢复 CasaOS 系统..."
if [ -f "casaos_state.tar.gz" ]; then
  sudo tar -xzf casaos_state.tar.gz -C / 2>/dev/null && echo "✅ CasaOS(gz) 恢复完成"
fi
if [ -f "casaos_state.tar.zst" ]; then
  sudo tar -I 'zstd -d' -xf casaos_state.tar.zst -C / 2>/dev/null && echo "✅ CasaOS(zstd) 恢复完成"
fi
if [ -f "docker_state.tar.gz" ]; then
  sudo tar -xzf docker_state.tar.gz -C / 2>/dev/null && echo "✅ Docker(gz) 恢复完成"
fi
if [ -f "docker_state.tar.zst" ]; then
  sudo tar -I 'zstd -d' -xf docker_state.tar.zst -C / 2>/dev/null && echo "✅ Docker(zstd) 恢复完成"
fi
echo "📌 恢复完成，建议重启服务: sudo systemctl restart docker casaos"
RESTORE_EOF
            chmod +x "$backup_dir/restore.sh"
            local distro=$(lsb_release -ds 2>/dev/null | sed -e 's/"/\\"/g' -e 's/\\/\\\\/g' || echo "unknown")
            cat > "$backup_dir/snapshot_info.json" <<EOF
{
  "timestamp": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")",
  "type": "full_backup",
  "runner_id": "${{ github.run_id }}",
  "components": {
    "docker": true,
    "casaos": true
  },
  "system_info": {
    "distro": "$distro",
    "kernel": "$(uname -r)",
    "arch": "$(uname -m)"
  }
}
EOF
            if [ -f "$backup_dir/docker_state.tar.${{ env.BACKUP_COMPRESSION }}" ] && [ -f "$backup_dir/casaos_state.tar.${{ env.BACKUP_COMPRESSION }}" ]; then
              echo "✅ 全量备份完成: $backup_dir"
              return 0
            else
              echo "❌ 备份文件验证失败"
              return 1
            fi
          }
          cleanup_old_backups() {
            local snapshot_dir="${{ env.WEBDAV_MOUNT_POINT }}/backups/snapshots"
            if [ -d "$snapshot_dir" ]; then
              echo "🧹 清理旧备份（保留最新${{ env.SNAPSHOT_RETENTION }}个）..."
              ls -1t "$snapshot_dir" 2>/dev/null | tail -n +$(( ${{ env.SNAPSHOT_RETENTION }} + 1 )) | while read old_snap; do
                [ -n "$old_snap" ] && echo "删除旧快照: $old_snap" && sudo rm -rf "$snapshot_dir/$old_snap"
              done
            fi
          }
          trigger_renewal() {
            USER_PAT="${{ secrets.USER_PAT }}"
            REPO="${{ github.repository }}"
            if [ -z "$USER_PAT" ] || [ -z "$REPO" ]; then
              echo "❌ 未配置USER_PAT，无法触发续期"
              return 1
            fi
            echo "🔄 触发工作流续期..."
            for i in 1 2 3; do
              response=$(curl -s -o /dev/null -w "%{http_code}" \
                -X POST \
                -H "Accept: application/vnd.github.v3+json" \
                -H "Authorization: token $USER_PAT" \
                "https://api.github.com/repos/$REPO/dispatches" \
                -d '{"event_type": "renew_casaos_tailscale"}')
              if [ "$response" = "204" ]; then
                echo "✅ 续期触发成功（新工作流已启动）"
                return 0
              fi
              echo "⚠️ 续期触发失败（第$i次），10秒后重试"
              sleep 10
            done
            echo "❌ 续期触发失败（重试3次）"
            return 1
          }
          echo "⏰ 开始监控运行时长（最大${{ env.MAX_RUN_MINUTES }}分钟）..."
          while true; do
            CURRENT_TIME=$(date +%s)
            RUN_MINUTES=$(( (CURRENT_TIME - START_TIME) / 60 ))
            REMAINING_MINUTES=$(( ${{ env.MAX_RUN_MINUTES }} - RUN_MINUTES ))
            echo "⏱️  已运行: ${RUN_MINUTES}分钟 | 剩余: ${REMAINING_MINUTES}分钟"
            if [ $RUN_MINUTES -ge 300 ] && [ "$BACKUP_DONE" = "false" ]; then
              echo "🚨 到达300分钟，执行全量备份..."
              if full_backup; then
                cleanup_old_backups
                trigger_renewal
                BACKUP_DONE=true
              else
                echo "❌ 首次备份失败，30秒后重试..."
                sleep 30
                if full_backup; then
                  cleanup_old_backups
                  trigger_renewal
                  BACKUP_DONE=true
                else
                  echo "❌ 重试备份仍失败，跳过续期"
                  BACKUP_DONE=true
                fi
              fi
            fi
            if [ $RUN_MINUTES -ge ${{ env.MAX_RUN_MINUTES }} ]; then
              echo "⏹️ 到达最大运行时长，退出"
              exit 0
            fi
            sleep 60
          done

      - name: 步骤9 - 清理环境（无论成败）
        if: always()
        run: |
          echo "===== 步骤9: 清理环境 ====="
          sudo fusermount -u "${{ env.WEBDAV_MOUNT_POINT }}" 2>/dev/null || sudo umount -lf "${{ env.WEBDAV_MOUNT_POINT }}" 2>/dev/null || true
          sudo rm -rf /tmp/* /var/tmp/*
          echo "✅ 环境清理完成"
          echo "结束时间: $(date)"
