name: CasaOS-Tailscale-Full-Deploy

on:
  workflow_dispatch:  # 手动触发
  repository_dispatch:
    types: [renew_casaos_tailscale]  # 仓库事件触发类型

jobs:
  deploy-casaos-tailscale:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    env:
      MAX_RUN_MINUTES: 360
      WEBDAV_MOUNT_POINT: /home/runner/webdav_mount
      BACKUP_DIRS: "/z/Ubu"
      CASAOS_DATA: /var/lib/casaos
      DOCKER_DATA: /var/lib/docker
      EXCLUDE_DIRS: >-
        /bin /boot /dev /lib /lib32 /lib64 /lost+found /proc /root /run /sbin /sys /tmp
        /var/cache /var/log /var/tmp /var/backups /home/runner /mnt /media
        /usr/share/doc /usr/share/man /usr/share/locale /etc/ssl/certs
        /etc/fstab /etc/mtab /etc/hostname /etc/machine-id
      MAX_MOUNT_RETRIES: 5  # 增加重试次数
      RETRY_DELAY: 10       # 重试间隔（秒）
      WEBDAV_URL: ${{ secrets.WEBDAV_URL }}
      WEBDAV_USER: ${{ secrets.WEBDAV_USER }}
      WEBDAV_PASSWORD: ${{ secrets.WEBDAV_PASSWORD }}
      USER_PAT: ${{ secrets.USER_PAT }}
      TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
      REPO: ${{ github.repository }}
      ROOTS_PASSWORD: ${{ secrets.ROOTS_PASSWORD || '随机生成' }}  # 安全优化：使用秘钥存储密码

    steps:
      # ========== 步骤1：系统环境初始化（使用 rclone） ==========
      - name: Step 1 - Initialize System Environment
        run: |
          set -e  # 出错立即退出
          echo "===== Step 1: Install rclone and dependencies ====="
          sudo apt update -y && sudo apt install -y rsync curl wget jq fuse3 || { echo "❌ Dependency installation failed"; exit 1; }
          # 安装rclone并验证
          curl https://rclone.org/install.sh | sudo bash
          if ! command -v rclone &> /dev/null; then
            echo "❌ rclone installation failed"
            exit 1
          fi
          # 配置fuse
          echo "user_allow_other" | sudo tee -a /etc/fuse.conf
          # 配置sudo权限（限制范围，提升安全）
          echo "runner ALL=(ALL) NOPASSWD: /usr/bin/rsync, /usr/bin/systemctl, /usr/bin/tailscale, /usr/bin/fusermount3, /usr/bin/docker" | sudo tee /etc/sudoers.d/runner
          sudo chmod 0440 /etc/sudoers.d/runner
          echo "✅ Initialization completed"

      # ========== 步骤1.5：创建 roots 用户（安全优化） ==========
      - name: Step 1.5 - Create roots User
        run: |
          set -e
          echo "===== Step 1.5: Create roots user ====="
          # 检查用户是否已存在
          if id "roots" &>/dev/null; then
            echo "ℹ️ roots user already exists"
          else
            sudo useradd -m -s /bin/bash roots
            # 使用秘钥或随机密码（避免硬编码）
            if [ -n "$ROOTS_PASSWORD" ] && [ "$ROOTS_PASSWORD" != "随机生成" ]; then
              echo "roots:$ROOTS_PASSWORD" | sudo chpasswd
            else
              RANDOM_PASS=$(openssl rand -base64 12)
              echo "roots:$RANDOM_PASS" | sudo chpasswd
              echo "🔑 roots random password: $RANDOM_PASS"
            fi
            sudo usermod -aG sudo roots
          fi
          echo "✅ Debug user 'roots' is ready"

      # ========== 步骤2：使用 rclone 挂载 WebDAV（增强重试） ==========
      - name: Step 2 - Mount WebDAV with rclone
        run: |
          set -e
          echo "===== Step 2: Mount WebDAV via rclone ====="
          # 空值检查
          if [ -z "$WEBDAV_URL" ] || [ -z "$WEBDAV_USER" ] || [ -z "$WEBDAV_PASSWORD" ]; then
            echo "❌ WebDAV secrets not configured"
            echo "WEBDAV_AVAILABLE=false" >> "$GITHUB_ENV"
            exit 0
          fi

          mkdir -p "$WEBDAV_MOUNT_POINT"
          # 配置rclone（加密密码）
          ENCRYPTED_PASS=$(rclone obscure "$WEBDAV_PASSWORD")
          rclone config create mywebdav webdav \
            url="$WEBDAV_URL" \
            vendor="other" \
            user="$WEBDAV_USER" \
            pass="$ENCRYPTED_PASS" \
            --config /home/runner/.rclone.conf

          # 挂载（增强稳定性）
          nohup rclone mount mywebdav: "$WEBDAV_MOUNT_POINT" \
            --config /home/runner/.rclone.conf \
            --vfs-cache-mode full \  # 改为full缓存，提升稳定性
            --daemon-timeout 1h \    # 延长守护进程超时
            --allow-other \
            --umask 022 \
            --log-file /tmp/rclone.log \
            --log-level DEBUG \      # 调试日志，便于排查
            --retries 3 \
            >/dev/null 2>&1 &

          # 增强重试逻辑
          MOUNT_SUCCESS=false
          for i in $(seq 1 $MAX_MOUNT_RETRIES); do
            echo "🔍 Checking mount attempt $i/$MAX_MOUNT_RETRIES..."
            if timeout 5 ls "$WEBDAV_MOUNT_POINT" >/dev/null 2>&1; then
              MOUNT_SUCCESS=true
              break
            fi
            sleep $RETRY_DELAY
          done

          if [ "$MOUNT_SUCCESS" = true ]; then
            echo "✅ rclone WebDAV mount succeeded"
            echo "WEBDAV_AVAILABLE=true" >> "$GITHUB_ENV"
            # 检查备份目录
            for dir in $BACKUP_DIRS; do
              target_path="$WEBDAV_MOUNT_POINT$dir"
              mkdir -p "$target_path"
              if [ -n "$(ls -A "$target_path" 2>/dev/null)" ]; then
                echo "✅ Valid backup detected: $dir"
                key="HAS_$(echo "$dir" | tr '/' '_' | tr -d '-')"
                echo "${key}=true" >> "$GITHUB_ENV"
              else
                echo "⚠️ Empty backup directory: $dir"
                key="HAS_$(echo "$dir" | tr '/' '_' | tr -d '-')"
                echo "${key}=false" >> "$GITHUB_ENV"
              fi
            done
          else
            echo "❌ rclone mount failed after $MAX_MOUNT_RETRIES retries"
            cat /tmp/rclone.log || echo "No rclone log found"
            echo "WEBDAV_AVAILABLE=false" >> "$GITHUB_ENV"
          fi

      # ========== 步骤3：数据恢复（增强容错） ==========
      - name: Step 3 - Restore Data
        if: ${{ env.WEBDAV_AVAILABLE == 'true' }}
        run: |
          set -e
          echo "===== Step 3: Restore Data ====="
          RESTORE_DONE=false
          # 验证rsync命令
          if ! command -v rsync &> /dev/null; then
            echo "❌ rsync not found"
            echo "DATA_RESTORED=false" >> "$GITHUB_ENV"
            exit 0
          fi

          for dir in $BACKUP_DIRS; do
            env_key="HAS_$(echo "$dir" | tr '/' '_' | tr -d '-')"
            if [ "${!env_key}" = "true" ]; then
              echo "🔄 Restoring system state from $dir..."
              EXCLUDE_PARAMS=()
              for excl in $EXCLUDE_DIRS; do
                EXCLUDE_PARAMS+=("--exclude=$excl")
              done
              # 排除挂载点自身
              EXCLUDE_PARAMS+=("--exclude=$WEBDAV_MOUNT_POINT")
              target_path="$WEBDAV_MOUNT_POINT$dir"

              # rsync恢复（添加进度和日志）
              sudo rsync -avP --delete --copy-links --sparse --numeric-ids \
                "${EXCLUDE_PARAMS[@]}" "$target_path/" / \
                --log-file=/tmp/rsync_restore.log
              
              if [ $? -eq 0 ]; then
                RESTORE_DONE=true
                echo "✅ Restore succeeded ($dir)"
                break
              else
                echo "⚠️ Restore $dir failed, log:"
                cat /tmp/rsync_restore.log || echo "No restore log"
              fi
            fi
          done
          echo "DATA_RESTORED=$RESTORE_DONE" >> "$GITHUB_ENV"

      # ========== 步骤4：安装 CasaOS ==========
      - name: Step 4 - Install or Skip CasaOS
        run: |
          set -e
          echo "===== Step 4: Install CasaOS ====="
          if [ -d "$CASAOS_DATA" ] && [ "$DATA_RESTORED" = "true" ]; then
            echo "✅ CasaOS data restored, skip installation"
          else
            # 安装前检查网络
            if ! curl -fsSL https://get.casaos.io >/dev/null; then
              echo "❌ Cannot reach CasaOS install server"
              exit 1
            fi
            curl -fsSL https://get.casaos.io | sudo bash || { 
              echo "❌ CasaOS installation failed"
              cat /var/log/casaos-install.log || echo "No CasaOS install log"
              exit 1
            }
            echo "✅ CasaOS installation completed"
          fi

      # ========== 步骤5：启动服务（增加重试） ==========
      - name: Step 5 - Start Services
        run: |
          set -e
          echo "===== Step 5: Start Docker + CasaOS ====="
          # 启动服务并重试
          for i in $(seq 1 3); do
            sudo systemctl enable --now docker casaos
            sleep 10
            if sudo systemctl is-active --quiet docker && sudo systemctl is-active --quiet casaos; then
              echo "✅ Services started successfully"
              break
            fi
            echo "⚠️ Service startup attempt $i failed, retrying..."
            sudo systemctl restart docker casaos
          done

          # 最终检查
          if ! sudo systemctl is-active --quiet docker || ! sudo systemctl is-active --quiet casaos; then
            echo "❌ Service startup failed after retries"
            echo "Docker status: $(sudo systemctl status docker --no-pager)"
            echo "CasaOS status: $(sudo systemctl status casaos --no-pager)"
            exit 1
          fi

      # ========== 步骤6：部署 Tailscale ==========
      - name: Step 6 - Deploy Tailscale
        run: |
          set -e
          echo "===== Step 6: Deploy Tailscale ====="
          if [ -z "$TAILSCALE_AUTH_KEY" ]; then
            echo "⚠️ TAILSCALE_AUTH_KEY not configured, skip networking"
            exit 0
          fi

          # 安装Tailscale
          curl -fsSL https://tailscale.com/install.sh | sudo bash
          if ! command -v tailscale &> /dev/null; then
            echo "❌ Tailscale installation failed"
            exit 1
          fi

          # 连接Tailscale（添加重试）
          for i in $(seq 1 3); do
            sudo tailscale up --authkey="$TAILSCALE_AUTH_KEY" --accept-routes --hostname="casaos-runner-${{ github.run_id }}"
            sleep 5
            TAILSCALE_IP=$(sudo tailscale ip -4)
            if [ -n "$TAILSCALE_IP" ]; then
              break
            fi
            echo "⚠️ Tailscale connection attempt $i failed"
          done

          if [ -z "$TAILSCALE_IP" ]; then
            echo "❌ Tailscale connection failed"
            echo "Tailscale log: $(sudo tailscale status)"
            exit 1
          fi
          echo "✅ Tailscale IP: $TAILSCALE_IP"
          echo "TAILSCALE_IP=$TAILSCALE_IP" >> "$GITHUB_ENV"

      # ========== 步骤7：输出访问信息 ==========
      - name: Step 7 - Print Access Info
        run: |
          set -e
          echo "===== Step 7: Access Information ====="
          # 获取公网IP（多源备份）
          PUBLIC_IP=$(curl -s --max-time 10 ifconfig.me || curl -s --max-time 10 icanhazip.com || echo "Unknown")
          echo "🔗 Public IP: http://$PUBLIC_IP"
          if [ -n "${{ env.TAILSCALE_IP }}" ]; then
            echo "🔒 Tailscale IP: http://${{ env.TAILSCALE_IP }}"
          fi
          echo "👤 Debug User: roots (password in secrets or random generated)"
          echo "⚠️ Please change CasaOS default password immediately!"

      # ========== 步骤8：保活 + 备份 + 续跑（修复逻辑） ==========
      - name: Step 8 - Keep Alive + Backup + Renew
        shell: bash
        run: |
          set -e
          echo "===== Step 8: Keep Alive + Backup + Renew ====="
          start_time=$(date +%s)
          RENEW_TRIGGERED=false  # 防止重复触发

          # 触发续跑工作流
          trigger_renew() {
            if [ -n "$USER_PAT" ] && [ -n "$REPO" ] && [ "$RENEW_TRIGGERED" = "false" ]; then
              echo "🔄 Triggering renew workflow..."
              RESPONSE=$(curl -fsS -X POST -H "Authorization: token $USER_PAT" \
                -H "Accept: application/vnd.github.v3+json" \
                "https://api.github.com/repos/$REPO/dispatches" \
                -d '{"event_type":"renew_casaos_tailscale"}' 2>&1)
              if [ $? -eq 0 ]; then
                echo "✅ Renew workflow triggered"
                RENEW_TRIGGERED=true
              else
                echo "❌ Failed to trigger renew: $RESPONSE"
              fi
            fi
          }
          
          # 备份单个目录
          backup_single_dir() {
            if [ "${WEBDAV_AVAILABLE}" != "true" ]; then
              echo "❌ WebDAV not available, skip backup"
              return 1
            fi
            local dir="$1"
            local target_path="$WEBDAV_MOUNT_POINT$dir"
            mkdir -p "$target_path"

            EXCLUDE_PARAMS=()
            for excl in $EXCLUDE_DIRS; do
              EXCLUDE_PARAMS+=("--exclude=$excl")
            done
            EXCLUDE_PARAMS+=("--exclude=$WEBDAV_MOUNT_POINT")
            
            echo "🔄 Starting backup to $dir ..."
            if sudo rsync -avP --delete --copy-links --sparse --numeric-ids \
                "${EXCLUDE_PARAMS[@]}" / "$target_path/" \
                --log-file=/tmp/rsync_backup.log; then
              # 写入备份信息（sudo确保权限）
              BACKUP_TIME="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
              RUNNER_ID="${{ github.run_id }}"
              GITHUB_REPO="${{ github.repository }}"
              WORKFLOW_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
              
              sudo printf '%s\n' \
                "backup_time=$BACKUP_TIME" \
                "runner_id=$RUNNER_ID" \
                "github_repo=$GITHUB_REPO" \
                "workflow_run_url=$WORKFLOW_URL" \
                > "$target_path/backup_info.txt"
              
              echo "✅ Backup completed ($dir)"
              return 0
            else
              echo "❌ Backup failed, log:"
              cat /tmp/rsync_backup.log || echo "No backup log"
              return 1
            fi
          }
          
          # 保活循环（修复逻辑）
          while true; do
            current_time=$(date +%s)
            run_mins=$(( (current_time - start_time) / 60 ))
            echo "📊 Running for $run_mins/$MAX_RUN_MINUTES minutes"
            
            # 剩余60分钟时触发备份和续跑（只触发一次）
            if [ $run_mins -ge 300 ] && [ "$RENEW_TRIGGERED" = "false" ]; then
              echo "⏰ 60 minutes remaining, starting backup and renew..."
              for dir in $BACKUP_DIRS; do
                if backup_single_dir "$dir"; then
                  trigger_renew
                  break
                fi
              done
            fi
            
            # 达到时间限制退出
            if [ $run_mins -ge $MAX_RUN_MINUTES ]; then
              echo "⏹️ Reached $MAX_RUN_MINUTES minutes limit, exiting"
              exit 0
            fi
            
            sleep 60
          done

      # ========== 步骤9：清理环境 ==========
      - name: Step 9 - Cleanup Environment
        if: ${{ always() }}
        run: |
          set -e
          echo "===== Step 9: Cleanup ====="
          # 安全卸载rclone
          if mount | grep -q "$WEBDAV_MOUNT_POINT"; then
            fusermount3 -u "$WEBDAV_MOUNT_POINT" 2>/dev/null || sudo fusermount3 -u "$WEBDAV_MOUNT_POINT" 2>/dev/null
          fi
          # 清理临时文件
          sudo rm -rf /tmp/* /var/tmp/* /home/runner/.rclone.conf 2>/dev/null || true
          # 清理sudo配置
          sudo rm -f /etc/sudoers.d/runner 2>/dev/null || true
          echo "✅ Cleanup completed"
