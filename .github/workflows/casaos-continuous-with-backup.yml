name: CasaOS-Tailscale-Full-Deploy

on:
  workflow_dispatch:
  repository_dispatch:
    types: [renew_casaos_tailscale]

jobs:
  deploy-casaos-tailscale:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    env:
      MAX_RUN_MINUTES: 360
      WEBDAV_MOUNT_POINT: /home/runner/webdav_mount
      BACKUP_DIRS: "/z/Ubu"
      CASAOS_DATA: /var/lib/casaos
      DOCKER_DATA: /var/lib/docker
      EXCLUDE_DIRS: >-
        /bin /boot /dev /lib /lib32 /lib64 /lost+found /proc /root /run /sbin /sys /tmp
        /var/cache /var/log /var/tmp /var/backups /home/runner /mnt /media
        /usr/share/doc /usr/share/man /usr/share/locale /etc/ssl/certs
        /etc/fstab /etc/mtab /etc/hostname /etc/machine-id
      MAX_MOUNT_RETRIES: 3
      RETRY_DELAY: 15
      WEBDAV_URL: ${{ secrets.WEBDAV_URL }}
      WEBDAV_USER: ${{ secrets.WEBDAV_USER }}
      WEBDAV_PASSWORD: ${{ secrets.WEBDAV_PASSWORD }}
      USER_PAT: ${{ secrets.USER_PAT }}
      TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
      REPO: ${{ github.repository }}

    steps:
      # ========== 步骤1：系统环境初始化（使用 rclone） ==========
      - name: 步骤1 - 系统环境初始化
        run: |
          echo "===== 步骤1：安装 rclone 和依赖 ====="
          sudo apt update -y && sudo apt install -y rsync curl wget jq fuse3 || { echo "❌ 依赖安装失败"; exit 1; }

          # 安装 rclone
          curl https://rclone.org/install.sh | sudo bash

          # 允许非 root 用户使用 allow_other
          echo "user_allow_other" | sudo tee -a /etc/fuse.conf

          # 配置 sudo 权限
          echo "runner ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/runner
          sudo chmod 0440 /etc/sudoers.d/runner
          echo "✅ 初始化完成"

      # ========== 步骤1.5：创建 roots 用户 ==========
      - name: 步骤1.5 - 创建 roots 用户
        run: |
          echo "===== 步骤1.5：创建 roots 用户 ====="
          sudo useradd -m -s /bin/bash roots
          echo "roots:roots1234" | sudo chpasswd
          sudo usermod -aG sudo roots
          echo "✅ 调试用户 'roots' 已就绪"

      # ========== 步骤2：使用 rclone 挂载 WebDAV ==========
      - name: 步骤2 - 使用 rclone 挂载 WebDAV
        run: |
          echo "===== 步骤2：rclone 挂载 WebDAV ====="
          mkdir -p "$WEBDAV_MOUNT_POINT"

          # 非交互式配置 rclone
          rclone config create mywebdav webdav \
            url="$WEBDAV_URL" \
            vendor="other" \
            user="$WEBDAV_USER" \
            pass="$WEBDAV_PASSWORD" \
            --config /home/runner/.rclone.conf

          # 后台挂载（关键：--allow-other + --vfs-cache-mode）
          nohup rclone mount mywebdav: "$WEBDAV_MOUNT_POINT" \
            --config /home/runner/.rclone.conf \
            --vfs-cache-mode writes \
            --daemon-timeout 5m \
            --allow-other \
            --umask 022 \
            --log-file /tmp/rclone.log \
            --log-level INFO \
            >/dev/null 2>&1 &

          # 等待挂载就绪（最多 20 秒）
          for i in $(seq 1 20); do
            if timeout 5 ls "$WEBDAV_MOUNT_POINT" >/dev/null 2>&1; then
              echo "✅ rclone WebDAV 挂载成功"
              echo "WEBDAV_AVAILABLE=true" >> "$GITHUB_ENV"

              for dir in $BACKUP_DIRS; do
                target_path="$WEBDAV_MOUNT_POINT$dir"
                if [ -d "$target_path" ] && [ -n "$(ls -A "$target_path" 2>/dev/null)" ]; then
                  echo "✅ 检测到有效备份: $dir"
                  key="HAS_${dir//\//_}"
                  echo "${key}=true" >> "$GITHUB_ENV"
                else
                  echo "⚠️ 初始化备份目录: $dir"
                  mkdir -p "$target_path"
                  key="HAS_${dir//\//_}"
                  echo "${key}=false" >> "$GITHUB_ENV"
                fi
              done
              exit 0
            fi
            sleep 1
          done

          echo "❌ rclone 挂载失败，日志如下："
          cat /tmp/rclone.log || echo "无日志"
          echo "WEBDAV_AVAILABLE=false" >> "$GITHUB_ENV"

      # ========== 步骤3：数据恢复 ==========
      - name: 步骤3 - 数据恢复
        if: ${{ env.WEBDAV_AVAILABLE == 'true' }}
        run: |
          echo "===== 步骤3：数据恢复 ====="
          RESTORE_DONE=false
          for dir in $BACKUP_DIRS; do
            env_key="HAS_${dir//\//_}"
            if [ "${!env_key}" = "true" ]; then
              echo "🔄 从 $dir 恢复系统状态..."
              EXCLUDE_PARAMS=()
              for excl in $EXCLUDE_DIRS; do
                EXCLUDE_PARAMS+=("--exclude=$excl")
              done
              target_path="$WEBDAV_MOUNT_POINT$dir"
              
              sudo rsync -av --delete --copy-links --sparse --numeric-ids \
                "${EXCLUDE_PARAMS[@]}" "$target_path/" /
              if [ $? -eq 0 ]; then
                RESTORE_DONE=true
                echo "✅ 恢复成功 ($dir)"
                break
              else
                echo "⚠️ 恢复 $dir 失败，尝试下一个（如有）"
              fi
            fi
          done
          echo "DATA_RESTORED=$RESTORE_DONE" >> "$GITHUB_ENV"

      # ========== 步骤4：安装 CasaOS ==========
      - name: 步骤4 - 安装或跳过 CasaOS
        run: |
          echo "===== 步骤4：安装 CasaOS ====="
          if [ -d "$CASAOS_DATA" ] && [ "${{ env.DATA_RESTORED }}" = "true" ]; then
            echo "✅ 检测到已恢复 CasaOS 数据，跳过安装"
          else
            curl -fsSL https://get.casaos.io | sudo bash || { echo "❌ CasaOS 安装失败"; exit 1; }
            echo "✅ CasaOS 安装完成"

      # ========== 步骤5：启动服务 ==========
      - name: 步骤5 - 启动服务
        run: |
          echo "===== 步骤5：启动 Docker + CasaOS ====="
          sudo systemctl enable --now docker casaos
          sleep 30
          if ! sudo systemctl is-active --quiet docker || ! sudo systemctl is-active --quiet casaos; then
            echo "❌ 服务启动失败"
            exit 1
          fi
          echo "✅ 服务运行中"

      # ========== 步骤6：部署 Tailscale ==========
      - name: 步骤6 - 部署 Tailscale
        run: |
          echo "===== 步骤6：部署 Tailscale ====="
          if [ -z "$TAILSCALE_AUTH_KEY" ]; then
            echo "⚠️ 未配置 TAILSCALE_AUTH_KEY，跳过组网"
            exit 0
          fi
          curl -fsSL https://tailscale.com/install.sh | sudo bash
          sudo tailscale up --authkey="$TAILSCALE_AUTH_KEY" --accept-routes --hostname="casaos-runner-${{ github.run_id }}"
          TAILSCALE_IP=$(sudo tailscale ip -4)
          if [ -z "$TAILSCALE_IP" ]; then
            echo "❌ Tailscale 连接失败"
            exit 1
          fi
          echo "✅ Tailscale IP: $TAILSCALE_IP"
          echo "TAILSCALE_IP=$TAILSCALE_IP" >> "$GITHUB_ENV"

      # ========== 步骤7：输出访问信息 ==========
      - name: 步骤7 - 访问信息
        run: |
          echo "===== 步骤7：访问信息 ====="
          PUBLIC_IP=$(curl -s --max-time 10 ifconfig.me)
          echo "🔗 公网: http://$PUBLIC_IP"
          if [ -n "${{ env.TAILSCALE_IP }}" ]; then
            echo "🔒 内网: http://${{ env.TAILSCALE_IP }}"
          fi
          echo "👤 调试用户: roots / roots1234"
          echo "⚠️ 请立即修改 CasaOS 默认密码！"

      # ========== 步骤8：保活 + 备份 + 续跑 ==========
      - name: 步骤8 - 保活与续跑
        shell: bash
        run: |
          echo "===== 步骤8：保活与续跑 ====="
          start_time=$(date +%s)

          trigger_renew() {
            if [ -n "$USER_PAT" ] && [ -n "$REPO" ]; then
              curl -fsS -X POST -H "Authorization: token $USER_PAT" \
                "https://api.github.com/repos/$REPO/dispatches" \
                -d '{"event_type":"renew_casaos_tailscale"}' >/dev/null && \
              echo "✅ 续跑任务已触发"
            fi
          }

          backup_single_dir() {
            if [ "${WEBDAV_AVAILABLE}" != "true" ]; then
              return 1
            fi
            local dir="$1"
            local target_path="$WEBDAV_MOUNT_POINT$dir"
            EXCLUDE_PARAMS=()
            for excl in $EXCLUDE_DIRS; do
              EXCLUDE_PARAMS+=("--exclude=$excl")
            done
            EXCLUDE_PARAMS+=("--exclude=$WEBDAV_MOUNT_POINT")

            echo "🔄 开始备份到 $dir ..."
            if sudo rsync -av --delete --copy-links --sparse --numeric-ids \
                "${EXCLUDE_PARAMS[@]}" / "$target_path/"; then

              BACKUP_TIME="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
              RUNNER_ID="${{ github.run_id }}"
              GITHUB_REPO="${{ github.repository }}"
              WORKFLOW_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

              printf '%s\n' \
                "backup_time=$BACKUP_TIME" \
                "runner_id=$RUNNER_ID" \
                "github_repo=$GITHUB_REPO" \
                "workflow_run_url=$WORKFLOW_URL" \
                > /tmp/backup_info.txt

              cp /tmp/backup_info.txt "$target_path/backup_info.txt"
              echo "✅ 备份完成 ($dir)"
              return 0
            fi
            return 1
          }

          while true; do
            current_time=$(date +%s)
            run_mins=$(( (current_time - start_time) / 60 ))
            echo "📊 运行 $run_mins/360 分钟"

            if [ $run_mins -eq 300 ]; then
              echo "⏰ 触发备份与续跑（剩余 60 分钟）"
              for dir in $BACKUP_DIRS; do
                if backup_single_dir "$dir"; then
                  trigger_renew
                  break
                fi
              done
            fi

            if [ $run_mins -ge 360 ]; then
              echo "⏹️ 达到 360 分钟上限，退出"
              exit 0
            fi

            sleep 60
          done

      # ========== 步骤9：清理环境 ==========
      - name: 步骤9 - 清理环境
        if: ${{ always() }}
        run: |
          echo "===== 步骤9：清理 ====="
          # 卸载 rclone
          fusermount3 -u "$WEBDAV_MOUNT_POINT" 2>/dev/null || true
          sudo rm -rf /tmp/* /var/tmp/* /home/runner/.rclone.conf || true
