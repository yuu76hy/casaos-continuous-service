name: CasaOS-Server-Snapshot-Backup-Restore

on:
  workflow_dispatch:
  repository_dispatch:
    types: [renew_casaos_snapshot]

jobs:
  casaos_snapshot_backup_restore:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    env:
      MAX_RUN_MINUTES: 360
      WEBDAV_SNAPSHOT_EN_DIR: "/z/Ubu"
      CORE_DIRS: "/var/lib /DATA /etc/casaos"
      EXCLUDE_RULES: >-
        --exclude=/var/lib/docker/overlay2/*/home/dependabot
        --exclude=/var/lib/docker/overlay2/*/vendor/ruby
        --exclude=/var/lib/docker/overlay2/*/gems
        --exclude=/var/lib/docker/containers/*/*.log
        --exclude=/var/log --exclude=/var/cache
        --exclude=*.log --exclude=*.tmp --exclude=*.cache
        --exclude=/tmp --exclude=/var/tmp
      WEBDAV_URL: ${{ secrets.WEBDAV_URL }}
      WEBDAV_USER: ${{ secrets.WEBDAV_USER }}
      WEBDAV_PASSWORD: ${{ secrets.WEBDAV_PASSWORD }}
      USER_PAT: ${{ secrets.USER_PAT }}
      REPO: ${{ github.repository }}
      ROOTS_PASSWORD: ${{ secrets.ROOTS_PASSWORD }}
      TRIGGER_BACKUP_AFTER_MINS: 10

    steps:
      - name: Env Init & Install Dependencies
        run: |
          set -e
          sudo apt update -y && sudo apt install -y curl wget jq fuse3 tar gzip locales openssl
          sudo locale-gen en_US.UTF-8
          export LC_ALL=en_US.UTF-8
          export LANG=en_US.UTF-8
          curl https://rclone.org/install.sh | sudo bash
          sudo sed -i 's/^Defaults requiretty/#Defaults requiretty/' /etc/sudoers
          echo "runner ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/runner
          sudo chmod 0440 /etc/sudoers.d/runner
          sudo mkdir -p /DATA && sudo chmod 755 /DATA
          echo "✅ Environment initialized"

      - name: Create roots User
        run: |
          set -e
          if ! id "roots" &>/dev/null; then
            sudo useradd -m -s /bin/bash roots
            if [ -n "$ROOTS_PASSWORD" ]; then
              echo "roots:$ROOTS_PASSWORD" | sudo chpasswd
            else
              RANDOM_PASS=$(openssl rand -base64 12)
              echo "roots:$RANDOM_PASS" | sudo chpasswd
              echo "✅ roots password: $RANDOM_PASS"
            fi
            sudo usermod -aG sudo roots
          fi

      - name: Configure Rclone for WebDAV (No Mount)
        run: |
          set -e
          export LC_ALL=en_US.UTF-8
          export LANG=en_US.UTF-8
          if [ -z "$WEBDAV_URL" ] || [ -z "$WEBDAV_USER" ] || [ -z "$WEBDAV_PASSWORD" ]; then
            echo "❌ Missing WebDAV secrets"
            echo "WEBDAV_AVAILABLE=false" >> "$GITHUB_ENV"
            exit 0
          fi
          ENCRYPTED_PASS=$(rclone obscure "$WEBDAV_PASSWORD")
          rclone config create mywebdav webdav \
            url="$WEBDAV_URL" \
            vendor="webdav" \
            user="$WEBDAV_USER" \
            pass="$ENCRYPTED_PASS" \
            --config /home/runner/.rclone.conf
          sudo chown runner:runner /home/runner/.rclone.conf
          sudo chmod 600 /home/runner/.rclone.conf
          if ! rclone ls mywebdav:/ --config /home/runner/.rclone.conf >/dev/null 2>&1; then
            echo "❌ WebDAV connection failed"
            echo "WEBDAV_AVAILABLE=false" >> "$GITHUB_ENV"
            exit 0
          fi
          echo "✅ WebDAV connected successfully (no mount mode)"
          echo "WEBDAV_AVAILABLE=true" >> "$GITHUB_ENV"
          rclone mkdir mywebdav:${WEBDAV_SNAPSHOT_EN_DIR} --config /home/runner/.rclone.conf
          echo "✅ Remote directory created: mywebdav:${WEBDAV_SNAPSHOT_EN_DIR}"
          REMOTE_SNAPSHOTS=$(rclone ls mywebdav:${WEBDAV_SNAPSHOT_EN_DIR} --config /home/runner/.rclone.conf | grep "casaos-server-snapshot-.*.tar.gz" | awk '{print $2}' | sort -r)
          if [ -n "$REMOTE_SNAPSHOTS" ]; then
            LATEST_SNAPSHOT_FILENAME=$(echo "$REMOTE_SNAPSHOTS" | head -n1)
            LATEST_SNAPSHOT="${WEBDAV_SNAPSHOT_EN_DIR}/${LATEST_SNAPSHOT_FILENAME}"
            echo "✅ Found latest remote snapshot: ${LATEST_SNAPSHOT}"
            echo "LATEST_SNAPSHOT=${LATEST_SNAPSHOT}" >> "$GITHUB_ENV"
          else
            echo "⚠️ No remote snapshots in mywebdav:${WEBDAV_SNAPSHOT_EN_DIR}"
          fi

      - name: Restore Snapshot Directly from WebDAV
        if: ${{ env.WEBDAV_AVAILABLE == 'true' && env.LATEST_SNAPSHOT != '' }}
        run: |
          set -e
          export LC_ALL=en_US.UTF-8
          export LANG=en_US.UTF-8
          echo "===== Restoring from remote snapshot: $LATEST_SNAPSHOT ====="
          
          sudo systemctl stop docker.service docker.socket || echo "⚠️ Docker service not running, skip stop"
          sudo systemctl mask docker.socket
          sudo pkill -9 docker || true
          sync && sleep 3
          
          if systemctl is-active --quiet casaos.service; then
            sudo systemctl stop casaos.service
            echo "✅ Stopped casaos.service"
          else
            echo "⚠️ casaos.service not loaded, skip stop"
          fi
          # 检查快照完整性
          if ! rclone cat mywebdav:${LATEST_SNAPSHOT} --config /home/runner/.rclone.conf | head -c 100 >/dev/null 2>&1; then
            echo "❌ Remote snapshot invalid or unreadable"
            echo "SNAPSHOT_RESTORED=false" >> "$GITHUB_ENV"
            sudo systemctl unmask docker.socket
            exit 1
          fi
          # 解压快照并保留详细日志
          rclone cat mywebdav:${LATEST_SNAPSHOT} --config /home/runner/.rclone.conf | sudo tar -xzf - -C / --overwrite --ignore-failed-read --warning=no-all $EXCLUDE_RULES 2>/tmp/restore_error.log
          if [ $? -eq 0 ] && [ ! -s /tmp/restore_error.log ]; then
            echo "✅ Restored from WebDAV successfully"
            echo "SNAPSHOT_RESTORED=true" >> "$GITHUB_ENV"
            rclone delete mywebdav:${LATEST_SNAPSHOT} --config /home/runner/.rclone.conf
            rclone delete mywebdav:${WEBDAV_SNAPSHOT_EN_DIR}/snapshot_meta.txt --config /home/runner/.rclone.conf
            echo "✅ Deleted restored snapshot file: ${LATEST_SNAPSHOT}"
          else
            echo "❌ Restore failed, error log:"
            cat /tmp/restore_error.log
            echo "SNAPSHOT_RESTORED=false" >> "$GITHUB_ENV"
          fi
          sudo systemctl unmask docker.socket

      - name: Install CasaOS if No Snapshot Restored
        if: ${{ env.SNAPSHOT_RESTORED != 'true' }}
        run: |
          set -e
          curl -fsSL https://get.casaos.io | sudo bash || {
            cat /var/log/casaos-install.log || true
            exit 1
          }
          echo "✅ CasaOS installed successfully"

      - name: Start Services (New Install)
        if: ${{ env.SNAPSHOT_RESTORED != 'true' }}
        run: |
          set -e
          sudo systemctl unmask docker.socket || true
          for i in $(seq 1 3); do
            sudo systemctl enable --now docker.service docker.socket
            sleep 10
            if sudo systemctl is-active --quiet docker && sudo systemctl is-active --quiet docker.socket; then
              echo "✅ Docker service started successfully"
              if systemctl list-unit-files | grep -q casaos.service; then
                sudo systemctl enable --now casaos.service
                sleep 15
                if sudo systemctl is-active --quiet casaos.service; then
                  echo "✅ CasaOS service started successfully"
                else
                  echo "❌ CasaOS start failed, status:"
                  sudo systemctl status casaos.service --no-pager
                  exit 1
                fi
              fi
              exit 0
            fi
            echo "⚠️ Docker start attempt $i failed, retrying..."
          done
          echo "❌ Docker service failed to start"
          exit 1

      - name: Restart Services (From Snapshot) with Debug
        if: ${{ env.SNAPSHOT_RESTORED == 'true' }}
        run: |
          set -e
          sudo systemctl unmask docker.socket || true
          sudo systemctl daemon-reload
          
          # Docker 重启 + 详细日志
          echo "🔍 Checking Docker service files..."
          ls -l /usr/lib/systemd/system/docker.service || true
          cat /usr/lib/systemd/system/docker.service || true
          
          for i in $(seq 1 3); do
            sudo systemctl enable --now docker.service docker.socket
            sleep 10
            if sudo systemctl is-active --quiet docker; then
              echo "✅ Docker service restarted from snapshot (attempt $i)"
              # 输出Docker状态和日志
              sudo systemctl status docker.service --no-pager
              sudo journalctl -u docker.service --no-pager -n 50
              break
            fi
            echo "⚠️ Docker restart attempt $i failed, cleaning processes..."
            sudo pkill -9 docker || true
            sudo rm -rf /var/run/docker.sock || true
            sync && sleep 5
          done
          
          if ! sudo systemctl is-active --quiet docker; then
            echo "❌ Docker service restart failed from snapshot"
            sudo journalctl -u docker.service --no-pager -n 100
            exit 1
          fi
          
          # CasaOS 重启 + 深度排查
          if systemctl list-unit-files | grep -q casaos.service; then
            echo "🔍 Checking CasaOS service files..."
            ls -l /etc/systemd/system/casaos.service || true
            cat /etc/systemd/system/casaos.service || true
            
            for i in $(seq 1 3); do
              sudo systemctl enable --now casaos.service
              sleep 20  # 延长等待时间，确保CasaOS加载完成
              if sudo systemctl is-active --quiet casaos.service; then
                echo "✅ CasaOS service restarted from snapshot (attempt $i)"
                # 输出CasaOS状态和日志
                sudo systemctl status casaos.service --no-pager
                sudo journalctl -u casaos.service --no-pager -n 50
                exit 0
              fi
              echo "⚠️ CasaOS restart attempt $i failed, cleaning..."
              sudo systemctl stop casaos.service || true
              sudo pkill -9 casaos || true
              sync && sleep 5
            done
            
            # CasaOS启动失败，输出完整日志用于排查
            echo "❌ CasaOS service restart failed after 3 attempts"
            echo "🔍 CasaOS service status:"
            sudo systemctl status casaos.service --no-pager
            echo "🔍 CasaOS system logs:"
            sudo journalctl -u casaos.service --no-pager -n 100
            echo "🔍 Checking CasaOS binary..."
            which casaos || true
            ls -l /usr/bin/casaos || true
            # 不终止流程，保留现场
          else
            echo "❌ casaos.service not found in snapshot"
            echo "🔍 Listing all installed services..."
            systemctl list-unit-files --type=service | grep -i casaos || true
            exit 1
          fi

      - name: Create Snapshot and Upload to WebDAV & Renew Workflow
        shell: bash
        run: |
          set -e
          export LC_ALL=en_US.UTF-8
          export LANG=en_US.UTF-8
          start_time=$(date +%s)
          RENEW_TRIGGERED=false
          SNAPSHOT_CREATED=false

          trigger_renew() {
            if [ -z "$USER_PAT" ] || [ -z "$REPO" ]; then
              echo "❌ USER_PAT or REPO is empty, skip renew"
              return 1
            fi
            echo "⏰ Triggering renewal workflow"
            if curl -fsSL --fail -X POST \
              -H "Authorization: token $USER_PAT" \
              -H "Accept: application/vnd.github.v3+json" \
              -H "Content-Type: application/json" \
              "https://api.github.com/repos/$REPO/dispatches" \
              -d '{"event_type":"renew_casaos_snapshot"}'; then
              echo "✅ Renewal workflow triggered successfully"
              RENEW_TRIGGERED=true
              return 0
            else
              echo "❌ Failed to trigger renewal workflow"
              return 1
            fi
          }

          create_and_upload_snapshot() {
            if [ "${WEBDAV_AVAILABLE}" != "true" ]; then
              echo "⚠️ WebDAV unavailable, skip snapshot"
              return 1
            fi
            SNAPSHOT_NAME="casaos-server-snapshot-$(date +%Y%m%d-%H%M%S).tar.gz"
            TMP_SNAPSHOT="/tmp/${SNAPSHOT_NAME}"
            REMOTE_PATH="mywebdav:${WEBDAV_SNAPSHOT_EN_DIR}/${SNAPSHOT_NAME}"
            echo "🔄 Creating snapshot: $SNAPSHOT_NAME"

            sudo systemctl stop docker.service docker.socket casaos.service || true
            sudo systemctl mask docker.socket
            sudo pkill -9 docker || true
            sync && sleep 5

            sudo tar -czf "${TMP_SNAPSHOT}" \
              --absolute-names \
              --ignore-failed-read \
              --warning=no-all \
              $EXCLUDE_RULES \
              $CORE_DIRS

            sudo chown runner:runner "${TMP_SNAPSHOT}"
            sudo chmod 644 "${TMP_SNAPSHOT}"

            if [ ! -f "${TMP_SNAPSHOT}" ] || [ $(stat -c%s "${TMP_SNAPSHOT}") -lt 1024 ]; then
              echo "❌ Snapshot is empty or invalid (size < 1KB)"
              cleanup_services
              return 1
            fi

            echo "✅ Snapshot created: $(du -sh ${TMP_SNAPSHOT} | awk '{print $1}')"
            if ! rclone copy "${TMP_SNAPSHOT}" "${REMOTE_PATH}" \
              --config /home/runner/.rclone.conf \
              --transfers 4 \
              --log-level INFO \
              --log-file /tmp/rclone_upload.log; then
              echo "❌ Upload failed, check log: /tmp/rclone_upload.log"
              cat /tmp/rclone_upload.log || true
              cleanup_services
              rm -f "${TMP_SNAPSHOT}"
              return 1
            fi

            echo "✅ Snapshot uploaded to WebDAV: ${REMOTE_PATH}"
            META_FILE="/tmp/snapshot_meta.txt"
            echo "snapshot_name=${SNAPSHOT_NAME}" > "${META_FILE}"
            echo "create_time=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "${META_FILE}"
            rclone copy "${META_FILE}" "mywebdav:${WEBDAV_SNAPSHOT_EN_DIR}/snapshot_meta.txt" --config /home/runner/.rclone.conf
            rm -f "${TMP_SNAPSHOT}" "${META_FILE}"
            cleanup_services
            return 0
          }

          cleanup_services() {
            sudo systemctl unmask docker.socket || true
            sudo systemctl start docker.service docker.socket || true
            if systemctl list-unit-files | grep -q casaos.service; then
              sudo systemctl start casaos.service || true
            fi
          }

          while true; do
            current_time=$(date +%s)
            elapsed_mins=$(( (current_time - start_time)/60 ))
            remaining_mins=$(( MAX_RUN_MINUTES - elapsed_mins ))

            echo "📊 Elapsed: $elapsed_mins mins | Remaining: $remaining_mins mins"

            if [ $elapsed_mins -eq $TRIGGER_BACKUP_AFTER_MINS ] && [ "$SNAPSHOT_CREATED" = "false" ]; then
              if create_and_upload_snapshot; then
                SNAPSHOT_CREATED=true
                trigger_renew
              fi
            fi

            if [ $elapsed_mins -ge $MAX_RUN_MINUTES ]; then
              echo "⏹️ Reached maximum run time, exiting"
              cleanup_services
              exit 0
            fi

            sleep 60
          done

      - name: Cleanup
        if: ${{ always() }}
        run: |
          set -e
          export LC_ALL=en_US.UTF-8
          export LANG=en_US.UTF-8
          sudo systemctl unmask docker.socket || true
          sudo rm -rf /tmp/casaos-server-snapshot-*.tar.gz /home/runner/.rclone.conf /etc/sudoers.d/runner /tmp/rclone_*.log /tmp/snapshot_meta.txt /tmp/restore_error.log
          echo "✅ Cleanup completed successfully"
